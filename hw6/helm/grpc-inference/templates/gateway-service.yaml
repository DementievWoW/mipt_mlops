apiVersion: v1
kind: Service
metadata:
  name: grpc-gateway-service
spec:
  selector:
    app: {{ include "grpc-inference.name" . }}-gateway
  ports:
  - port: {{ .Values.gateway.service.port }}
    targetPort: grpc
    protocol: TCP
    name: grpc
